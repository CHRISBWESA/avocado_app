services:
  - type: web
    name: avocado-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server/app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
